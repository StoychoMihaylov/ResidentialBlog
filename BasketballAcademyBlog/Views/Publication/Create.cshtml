@model ResidentialBlog.Models.Publication
@{
    ViewBag.Title = "Създаване на тема";
}

<div class="container">
        <h2>Създай тема</h2>
        @using (Html.BeginForm("Create", "Publication", FormMethod.Post, new { @class = "form-horizontal" }))
        {
            @Html.AntiForgeryToken()
            @Html.ValidationSummary("", new { @class = "text-danger" })

            <div class="form-group">
                @Html.LabelFor(m => m.Title, "Заглавие", new { @class = "control-label col-sm-4" })
                <div class="col-sm-4">
                    @Html.TextBoxFor(m => m.Title, new { @class = "form-control" })
                </div>
            </div>
            <div class="form-group">
                @Html.LabelFor(m => m.Content, "Съдържание", new { @class = "control-label col-sm-4" })
                <div class="col-sm-4">
                    @Html.TextAreaFor(m => m.Content, new { @class = "form-control", @rows = "7" })
                </div>
            </div>
            <div class="form-group">
                <div class="col-sm-4 col-sm-offset-4">
                    @Html.ActionLink("Остави", "Index", "Publication", null, new { @class = "btn btn-default" })
                    <input type="submit" value="Съзай" class="btn btn-success" />
                </div>
            </div>
        }
</div>


